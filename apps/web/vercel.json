{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "corepack enable && pnpm --dir ../.. install --frozen-lockfile=false",
  "buildCommand": "pnpm --dir ../.. --filter @copyr/web build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
